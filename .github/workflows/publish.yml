name: Publish container image

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: false

env:
  REGISTRY: org-01kkkppnnytj41vh4x02et4a8q.registry-stage.airelabs.studio
  IMAGE_NAME: lcoe-r
  # Cloudflare Free/Pro caps request bodies at 100 MB; 50 MB leaves headroom
  # for HTTP overhead. Without this, regctl uploads each blob as a single
  # PUT and any layer >=100 MB 413s at the edge — same failure mode as
  # `docker push`. See examples/container-function-r-slow/push.sh.
  BLOB_CHUNK_BYTES: "50000000"

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install regctl
        uses: regclient/actions/regctl-installer@v0.1.0

      - name: Log in to Aire Labs registry
        run: |
          printf '%s' "${{ secrets.AIRE_API_KEY }}" \
            | regctl registry login "$REGISTRY" --user api-key --pass-stdin

      - name: Configure chunked uploads
        run: |
          regctl registry set "$REGISTRY" \
            --blob-max "$BLOB_CHUNK_BYTES" \
            --blob-chunk "$BLOB_CHUNK_BYTES"

      - name: Build image (linux/amd64)
        run: |
          docker build --platform linux/amd64 -t "$IMAGE_NAME:${GITHUB_REF_NAME}" .

      - name: Export image to tarball
        run: |
          docker save "$IMAGE_NAME:${GITHUB_REF_NAME}" -o image.tar

      - name: Push image with regctl (chunked)
        run: |
          regctl image import \
            "$REGISTRY/$IMAGE_NAME:${GITHUB_REF_NAME}" \
            image.tar
